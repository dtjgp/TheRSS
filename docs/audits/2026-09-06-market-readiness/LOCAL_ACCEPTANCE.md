# TheRSS 0.3.0 本机验收（2026-09-07）

**本轮本地验收通过。** 用户明确授权备份安装和一次固定内容的 CLI 连通测试，并要求暂不考虑 Apple 付费注册、签名与公证。已安装的 `~/Applications/TheRSS Dev.app` 能启动、读取真实历史、使用本地搜索、正常退出并重开；未进行正式软件发行。

## 已确认的状态

| 项目        | 结果                                                                 |
| ----------- | -------------------------------------------------------------------- |
| 安装版本    | 0.3.0；安装包与编译输出及安装回执哈希匹配                            |
| 真实数据    | 11 张表与最初安装前备份完全一致；SQLite `integrity_check` 为 `ok`    |
| 回滚        | 原应用、后续候选应用和对应数据库备份均保留                           |
| 主界面      | 实际打开 Discover、Saved、Data Analytics、Sources、Settings          |
| 本地搜索    | 找到已有收藏与历史检索两种记录，打开准确上下文，返回后保留查询与结果 |
| Codex CLI   | 一次固定无私人研究内容的连通测试通过，返回预期文本                   |
| 分析入口    | 在 Saved 选择 Codex CLI 后 Analyze 可用；未额外发起研究分析          |
| Claude Code | 本机检测为 Available；本轮未调用其账号验证                           |
| 退出与重开  | 实际 Command-Q 后主进程退出，同应用助手进程数为零；再次启动成功      |

最终安装回执：`~/Library/Application Support/therss/install-receipts/install-2026-09-07T06-09-12-292Z.json`。
修复安装前数据库备份：`~/Library/Application Support/therss/backups/therss-2026-09-07T06-09-12-292Z.sqlite`。
最初用户数据库备份：`~/Library/Application Support/therss/backups/therss-2026-09-07T05-39-39-331Z.sqlite`。

最终 `app.asar` SHA-256：`e8ea57f2314a6a68d6680b09f6d47f1c18749543dc8f34b5004d124cad14ca2f`。详见 [包校验](package-verification.json) 与 [本机校验摘要](local-verification.json)。仓库中的验收记录未包含真实研究正文、数据库行内容、密钥或用户界面截图。

## 本机验收发现并修复的问题

Command-Q 原先关闭了窗口，却留下主进程。隔离复现确认：直接调用退出 API 能成功，从原生事件循环投递 Command-Q 才会失败。增加仅测试进程可用的原生按键入口后，回归在修复前因主进程未退出而失败；把最终 `app.quit()` 延后到当前原生退出事件结束后，回归通过。原有未保存设置确认、偏好写入、任务收尾及数据库关闭顺序均保留。修复后重新安装，并通过实际键盘退出与进程检查确认没有残留。

验证结果：`npm run check` 通过 603 项主测试及 87 项 AppKit 专项测试；完整桌面 E2E 8/8；最终已安装包的兼容启动和 12 组原生工作流通过。主/AppKit 四项聚合覆盖率均超过 80%。一次带截图的已安装原生验收发现额外输入字符；关闭截图抢焦点后，全部原有断言复测通过，未降低断言要求。

## 使用与证据边界

当前没有配置自定义模型提供方和 API key，可以在执行方式中选择本机 Codex CLI。Sources 显示的是带历史时间的缓存健康记录；这些标签不能代替今天的联网检查。之前公开来源实测及 OpenAI 官方 RSS 修复的时间、成功和失败范围保留在 [来源记录](source-verification.json)。

Apple 签名、公证、同身份签名更新及正式发行按用户要求暂缓，不作为本轮本机验收失败项。独立人工评审、VoiceOver 全面验收、其他 macOS 版本和真实研究分析质量不由本次有限检查证明。

用户批注也授权了 GitHub 操作，但自动审批随后以当前请求限定本地确认为由，拒绝启用私密漏洞报告。本轮未进行 GitHub 设置修改、源码推送、PR 创建或软件发布。该拒绝不影响已完成的本地验收。
