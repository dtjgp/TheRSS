# Complete native research workflows and reliability

Native notices shifted the reader, compact layouts lost reading space, and local search could not reopen the exact stored context. This change stabilizes feedback, preserves selection and scroll positions, adds validated Saved/session/analysis navigation, and improves reading actions, settings recovery, source selection and numeric Analytics tables.

Source parsing, redirects, cancellation and complete-bundle installation checks are hardened. OpenAI and three previously timing-out news sources now use official RSS/list endpoints. All-rejected source batches fail without clearing cached items, source browsing records health without inflating search counts, update dates are labeled accurately, and generic analysis v2 separates popularity from research evidence. Native reading rechecks changed metadata for the same record ID and compares the displayed snapshot with the analysis source. A reproduced native Command-Q re-entry race is also fixed.

Validation: 630 main tests and 90 AppKit tests, each aggregate coverage threshold above 80%; desktop E2E 8/8; exact packaged and installed native workflows 12/12. Real Codex/Claude planning, analysis, Save and reopen passed in an isolated public-data profile. The initially incorrect Claude inference is retained as a failed case; the corrected v2 follow-up passed its bounded review. All 11 actual user-data tables match the immediate install backup. Package fingerprints and compiled modules match. Dependencies are unchanged from the recorded zero-vulnerability audit.

Version 0.3.0 remains an unsigned local candidate. NBER publication-date enrichment failed and NCPSD remains partial, with failure/cache behavior preserved. Independent human acceptance is pending. Custom paid model APIs, VoiceOver and formal Apple distribution are deferred by the user. See `REMAINING_ACCEPTANCE.md`, `remaining-verification.json`, `HUMAN_ACCEPTANCE.md` and `REPORT.md` for provenance and limits.
